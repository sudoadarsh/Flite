The generator for the [`flite`](https://pub.dev/packages/flite) package.

Run the following command to add the package as a developer package to your project.
```
flutter pub add --dev flite_generator
```